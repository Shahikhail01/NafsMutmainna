# NafsMutmainna Environment Variables
# Copy this to .env and fill in values — never commit .env to git

# Backend API URL
API_URL=https://api.nafsmutmainna.com/v1

# MiniMax-M2.5 API Key (via your backend — not used directly in app)
MINIMAX_API_KEY=your_minimax_key_here